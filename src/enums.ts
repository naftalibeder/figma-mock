export enum Casing {
  Original = 'original',
  Sentence = 'sentence',
  Title = 'title',
  Upper = 'upper',
  Lower = 'lower',
}

export enum Sort {
  Original = 'original',
  Random = 'random',
  Ascending = 'ascending',
  Descending = 'descending',
}

export enum ListType {
  Strings = 'strings',
  Numbers = 'numbers',
  Dates = 'dates',
}