export enum Casing {
  None = 'none',
  Sentence = 'sentence',
  Title = 'title',
  Upper = 'upper',
  Lower = 'lower',
}

export enum Sort {
  Random = 'random',
  Ascending = 'ascending',
  Descending = 'descending',
}